name: Image ou Video
icon: image
fields:
  image:
    label: Image
    type: files
    uploads: image
    query: site.images.add(page.images)
    max: 1
  format:
    label: format
    type: radio
    options:
      100%: Pleine page
      50%: Moitiée de page
  caption:
    label: texte associé à l'image
    type: writer
    nodes: false
    marks: 
      - link
      - email